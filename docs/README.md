# UsedIt - Vocabulary Practice App

UsedIt is a modern vocabulary practice application that focuses on helping users master English words through real-world context and conversational scenarios. Instead of just memorizing definitions, users engage in simulated daily-life conversations generated by Local AI (Ollama) and receive instant, AI-driven feedback on their grammar, meaning, and naturalness.

## Tech Stack

- **Frontend**: React, TypeScript, Vite, TailwindCSS (v4)
- **Backend**: FastAPI, SQLModel (SQLite)
- **AI / LLM**: LangChain, ChatOllama (`llama3.1:8b`)
- **Vector Database**: ChromaDB (for real-world sentence collocations)

---

## Project Structure

The project has been structured according to standard software engineering practices:

```text
UsedIt/
├── docs/                     # Documentation and manuals
│   └── UsedIt_产品手册.docx   # Product manual
├── frontend/                 # React frontend
│   ├── src/
│   │   ├── components/       # UI Components
│   │   ├── config/           # Configuration (e.g., API Base URL)
│   │   ├── pages/            # Page Views
│   │   └── types/            # TypeScript interfaces
│   └── package.json          # Node dependencies
└── backend/                  # FastAPI backend
    ├── app/                  # Application code (routers, models, database)
    ├── data/                 # Databases and datasets
    │   ├── chroma_db/        # ChromaDB vector database storage
    │   ├── dev.db            # SQLite relational database
    │   ├── raw/              # Raw datasets (e.g., eng_sentences.tsv)
    │   └── processed/        # Processed datasets (e.g., word_examples.csv)
    ├── scripts/              # Data automation and maintenance scripts
    │   ├── automation_pipeline.py
    │   ├── read_chromadb.py
    │   └── setup_chroma.py
    └── requirements.txt      # Python dependencies
```

---

## Prerequisites

Before running the project, ensure you have the following installed:
1. **Node.js** (v18+) for the frontend.
2. **Python 3.10+** for the backend.
3. **Ollama** installed and running locally with the `llama3.1:8b` model pulled:
   ```bash
   ollama pull llama3.1:8b
   ```

---

## Getting Started

### 1. Running the Backend

The backend is built with FastAPI and uses SQLite and ChromaDB.

1. Navigate to the backend directory:
   ```bash
   cd backend
   ```
2. Install dependencies (it is recommended to use a virtual environment):
   ```bash
   pip install -r requirements.txt
   pip install chromadb langchain-ollama requests
   ```
3. Start the FastAPI server:
   ```bash
   uvicorn app.main:app --reload
   ```
   The backend will be available at `http://localhost:8000`.

### 2. Running the Frontend

The frontend is a Vite-powered React application.

1. Navigate to the frontend directory:
   ```bash
   cd frontend
   ```
2. Install NPM dependencies:
   ```bash
   npm install
   ```
3. Start the development server:
   ```bash
   npm run dev
   ```
   The frontend will be available at `http://localhost:5173`.

---

## Data Management & Scripts

The `backend/scripts/` directory contains tools for managing the vector database (ChromaDB) which provides real-world collocation examples for words.

- **`automation_pipeline.py`**: Extracts example sentences from the raw `eng_sentences.tsv`, saves them to a CSV in `backend/data/processed/`, and ingests them into the Chroma vector database.
- **`read_chromadb.py`**: A utility script to quickly inspect the contents of the `vocab-examples` collection inside ChromaDB to verify data insertion.
- **`setup_chroma.py`**: A basic script to manually test ChromaDB initialization and insert a few dummy examples.

To run any of the scripts, execute them from the project root or backend directory. For example:
```bash
python3 backend/scripts/read_chromadb.py
```
